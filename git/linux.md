```
[root@localhost googlesearch]# git --help
usage: git [--version] [--exec-path[=GIT_EXEC_PATH]] [--html-path]
           [-p|--paginate|--no-pager] [--no-replace-objects]
           [--bare] [--git-dir=GIT_DIR] [--work-tree=GIT_WORK_TREE]
           [--help] COMMAND [ARGS]

The most commonly used git commands are:
           add        Add file contents to the index | 将文件内容添加到索引中
           bisect     Find by binary search the change that introduced a bug | 通过二进制搜索查找引入错误的更改
           branch     List, create, or delete branches | 列出，创建或删除分支
           checkout   Checkout a branch or paths to the working tree | 签出一个或多个工作树的路径
           clone      Clone a repository into a new directory | 将存储库克隆到新目录中
           commit     Record changes to the repository | 将更改记录到存储库
           diff       Show changes between commits, commit and working tree, etc | 显示提交，提交和工作树之间的更改等
           fetch      Download objects and refs from another repository | 从另一个存储库下载对象和引用
           grep       Print lines matching a pattern | 打印与图案匹配的线条
           init       Create an empty git repository or reinitialize an existing one | 创建一个空的git存储库或重新初始化现有的存储库
           log        Show commit logs | 显示提交日志
           merge      Join two or more development histories together | 将两个或多个开发历史连接在一起
           mv         Move or rename a file, a directory, or a symlink | 移动或重命名文件，目录或符号链接
           pull       Fetch from and merge with another repository or a local branch | 从另一个存储库或本地分支获取并合并
           push       Update remote refs along with associated objects | 更新远程引用以及关联的对象
           rebase     Forward-port local commits to the updated upstream head | 转发端口本地提交到更新的上游头
           reset      Reset current HEAD to the specified state | 将当前HEAD重置为指定状态
           rm         Remove files from the working tree and from the index | 从工作树和索引中删除文件
           show       Show various types of objects | 显示各种类型的对象
           status     Show the working tree status | 显示工作树状态
           tag        Create, list, delete or verify a tag object signed with GPG | 列出，删除或验证使用GPG签名的标记对象

See 'git help COMMAND' for more information on a specific command.
[root@localhost googlesearch]
```

克隆项目

```
git clone  https://github.com/MarioVilas/googlesearch.git
```



